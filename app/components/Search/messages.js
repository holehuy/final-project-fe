import { defineMessages } from 'react-intl';

export const scope = 'evaluationSystem.components.Search';

export default defineMessages({
  placeholder: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Search here...',
  },
});
