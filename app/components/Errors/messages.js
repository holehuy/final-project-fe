import { defineMessages } from 'react-intl';

export const scope = 'evaluationSystem.components.Errors';

export default defineMessages({
  buttonGoBack: {
    id: `${scope}.buttonGoBack`,
    defaultMessage: 'Go to Dashboard',
  },
});
