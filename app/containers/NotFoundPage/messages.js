import { defineMessages } from 'react-intl';

export const scope = 'evaluationSystem.containers.NotFoundPage';

export default defineMessages({
  titleNotFound: {
    id: `${scope}.titleNotFound`,
    defaultMessage: 'Page Not Found',
  },
});
